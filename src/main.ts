import './style.css'
import './card-reader'

// The CardReader web component is automatically registered
// when imported, so we just need to make sure it's loaded
console.log('Card Scanner initialized');
